export default function About() {
  return (
    <div className="card shadow-sm">
      <div className="card-body">
        <h3>About This App</h3>
        <p className="mb-0">
          This survey app collects basic user details first, then allows the
          user to answer questions, review everything on a confirmation page,
          and finally submit the survey.
        </p>
      </div>
    </div>
  );
}
